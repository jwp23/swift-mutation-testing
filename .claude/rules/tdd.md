# TDD
Use red/green TDD for every feature/bugfix: write a failing test, write the minimum code to
pass, refactor while green.
Exemptions: shell/config scripts, Markdown/docs, YAML/CI workflow files, generated fixture
output.
